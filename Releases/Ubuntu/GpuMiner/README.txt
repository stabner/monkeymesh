MonkeyMesh GPU Miner (Ubuntu CLI)

This folder is the Linux CLI miner (mesh-miner-gpu), pulled from the
seed NAS build. Edit config.json, then:

  chmod +x ./mesh-miner-gpu ./start-gpu-miner.sh
  ./start-gpu-miner.sh
