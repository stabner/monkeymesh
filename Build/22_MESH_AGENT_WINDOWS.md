# MonkeyMesh Agent (Windows Node GUI)

**Status: REMOVED.**

The Agent chat / builder panel is gone from the Node GUI and orchestrator. GPU market work is **protocol research only** (Build/21).

Prefer:

```powershell
.\Launchers\smoke-adaptive-auto.ps1
.\Launchers\smoke-research.ps1
```

## Related

- Build/21 — self-adaptive mining (active)
- Build/18 — adaptive protocol research scenarios
- Build/15 — MeshPulse adaptive loop
