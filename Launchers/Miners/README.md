# MonkeyMesh Miners (lab scripts)

Ship pack: `Releases/Windows/Miner/` — one GUI for CPU + NVIDIA + AMD.

```
.\Launchers\stage-miner-releases.ps1
```

Lab scripts under `Launchers/Miners/` still work against `bin\` for local testing.
Set payout `"address"` in `config.json` (gitignored).
